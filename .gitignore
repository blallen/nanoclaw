# Dependencies
node_modules/

# Build output
dist/

# Local data & auth
store/
data/
logs/

# Groups - only track base structure and specific CLAUDE.md files
groups/*
!groups/main/
!groups/global/
groups/main/*
groups/global/*
!groups/main/CLAUDE.md
!groups/global/CLAUDE.md
# Generated per-run MCP config (machine-specific absolute paths) — never commit
groups/*/.mcp.json
# But DO commit the static .claude config (settings + ported hooks) for groups/main
!groups/main/.claude/

# Secrets
*.keys.json
.env

# OS
.DS_Store

# IDE
.idea/
.vscode/

agents-sdk-docs

# mcp stuff that is big
vendor/CheICalMCP
